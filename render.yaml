services:
  - type: web
    name: music-player-api
    runtime: node
    plan: free
    buildCommand: cd gequbao-proxy && npm install
    startCommand: cd gequbao-proxy && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
